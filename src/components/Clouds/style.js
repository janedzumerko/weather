import { styled } from '@material-ui/styles';

export const Wrapper = styled('div')({
	position: 'absolute',
	top: '0',
	left: '0',
	right: '0',
	bottom: '0',
	overflow: 'hidden',
	transform: 'translateZ(0)',
});
